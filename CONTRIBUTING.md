# Contributing to HerVoice

- Use branches for all changes.
- Use conventional commits (`feat:`, `fix:`, etc.)
- Open a Pull Request (PR) before merging to main.
- Include a clear description in all PRs.
